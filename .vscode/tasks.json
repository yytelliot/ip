{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "rm -rf bin && mkdir -p bin && javac -d bin $(find src/main/java -name \"*.java\")",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$javac"],
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      }
    }
  ]
}
